import { last } from "rxjs/operators";

describe('first tests', () => {
    it('should be true', () => {
        expect(true).toBe(true);
    });

    it('should equal 2', () => {
        expect(2).toBe(2);
    });
})